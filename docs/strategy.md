# 戦略 — Open Ground Coffee

## ジャンル実験（日次 PDCA の上位層）— 毎朝ルーチンは最初にこの節を読む

> **正本**: sns-hub [`docs/strategy/genre-experiment.md`](https://github.com/nannantown/sns-hub/blob/main/docs/strategy/genre-experiment.md)（2026-09-14 オーナー決定「AI系 IG 以外の 5 アカウントはジャンル/型を変えながら試す。コーヒーは販促維持」）。この節はこのリポの 2 アカウント分の**写し**で、食い違ったら正本が勝つ。
> 下の「Meta PDCA」は「このジャンルの中でどのネタ・method が効くか」を毎日最適化する層。この節は「**このジャンル/型そのものを続けるか切るか**」を 14 日ごとに判定する上位層で、**Meta PDCA より優先する**。
> この節の書き換えは人間だけ（ルーチンは提案まで）。IG と YT は**別アカウント**として別々に評価し、数字を合算しない。

### このリポの試行台帳（写し）

| アカウント | 試行 # | ジャンル / 型 | 開始日 S | 型の初回投稿日 F | 導入時（2026-09-14）の判定 |
|---|---|---|---|---|---|
| IG @open_ground_coffee_roasters | #0 | 平日: 英語圏コーヒーニュース / 週末: エバーグリーン / タイトル固定「【コーヒー豆知識】…｜日付 #Shorts」 | 2026-09-14 | 2026-04-20 | 配信死亡（09-01..09-14: views 中央値 2.5 / 保存合計 0, n=12） |
| YT OPEN GROUND coffee roasters (@OpenGroundCoffee) | #0 | 同上 | 2026-09-14 | 2026-04-20 | 配信死亡（09-01..09-14: views 中央値 0, n=12） |

- 試行 #0 = 2026-04 からの現行型。S はルール導入日、F は現行型を始めた日
- 新しい型を始める PR では、sns-hub の台帳とこの表を**同じ日に**書き換える（試行 # を進め、S = F = 新しい型の初回投稿日）
- 試行 #1 の候補（2026-09-14 時点で別カードが実装中）: レシピカード型

### 判定窓・経過日・次の判定日

```
d = 今日 − S（日数。日付はすべて JST）
d ≥ 14 かつ d が 14 の倍数 → 今日は判定日。判定窓 = (今日 − 14)..(今日 − 1)、次の判定日 = 今日 + 14
それ以外                  → 経過日 = Day (d mod 14 + 1) / 14、判定窓 = max(F, 今日 − 13)..今日、
                             次の判定日 = S + 14 × (floor(d / 14) + 1)
```

例: 09-15 は Day 2 / 14・判定窓 09-02..09-15・次の判定日 09-28。09-28 は判定日（判定窓 09-14..09-27）。

### 判定指標と集計コマンド

- **IG**: 判定窓の `instagram.views` の**中央値** + `instagram.saved` の**合計**（`instagram` か `instagram.views` が null の回は除外）
- **YT**: 判定窓の `stats.views` の**中央値**（`stats.updatedAt` が null の回は除外）
- n は IG / YT で別々に数える。**n < 7 なら判定保留**。IG と YT を足したり平均したりしない

```bash
# FROM / TO = 判定窓（上の計算式）
FROM=2026-09-02; TO=2026-09-15
jq -r --arg from "$FROM" --arg to "$TO" '
  def median: sort | if length==0 then null elif length%2==1 then .[(length/2|floor)] else ((.[length/2-1]+.[length/2])/2) end;
  [.videos[] | select(.date >= $from and .date <= $to)] as $v
  | ($v | map(select(.stats.updatedAt != null) | (.stats.views // 0))) as $yt
  | ($v | map(select(.instagram != null and .instagram.views != null) | .instagram)) as $ig
  | "window=\($from)..\($to) posts=\($v|length)",
    "YT views median=\($yt|median) (n=\($yt|length))",
    "IG views median=\($ig|map(.views)|median) saved_sum=\($ig|map(.saved // 0)|add // 0) (n=\($ig|length))"
' data/performance-history.json
```

### 閾値（表の上から順に当てはめ、最初に該当した行が判定。初期値・仮置き）

| 判定 | IG | YT |
|---|---|---|
| **配信死亡** | views 中央値 < 10 **かつ** 保存合計 < 5 | views 中央値 < 5 |
| **切替候補** | views 中央値 < 50 **かつ** 保存合計 < 5 | views 中央値 < 20 |
| **続行** | 上記以外 | 上記以外 |

判定は指標だけで機械的に出す（「もう少し様子を見たい」は理由にしない）。閾値はルーチンが変えない（おかしいと思ったら「戦略更新提案」に書く）。

### モードの決め方（毎朝、アカウントごと）

モードは `通常` / `切替候補` / `配信死亡モード` の 3 つ。

1. **前回モード** = `docs/pdca/` の最新レポートの「ジャンル試行の状態」のモード列。節が無ければ上の表の「導入時の判定」（配信死亡 → `配信死亡モード（2026-09-14〜）`）。試行 # が前回と違えば引き継がず `通常` から
2. **今日の判定値** = 判定窓の指標を閾値に当てはめた結果（n < 7 なら判定保留）
3. **決定**（上から順に）:
   - 判定保留 → 前回モードのまま
   - 前回が `配信死亡モード` → **判定日かつ判定値が配信死亡以外**のときだけ出る（続行 → `通常`、切替候補 → `切替候補`）。それ以外は `配信死亡モード` のまま
   - 判定値が配信死亡 → `配信死亡モード` に入る（判定日でなくても。宣言日 = 今日）
   - 今日が判定日 → 判定値どおり（続行 → `通常`、切替候補 → `切替候補`）
   - それ以外 → 前回モードのまま

### 配信死亡モード中の日次 PDCA

1. **method 切り口の最適化を止める**: 配信死亡モードのアカウントの数字で method の TOP/WORST 比較・Method 提案・80/20 選定・WORST method の凍結をしない（0 と 1 を比べても何も分からない）
   - **2 アカウントとも配信死亡モード** → method は**性能データで選ばない**。コンテンツ柱比率・曜日フォーマット・連続回避・NG パターンの制約だけで選ぶ（method 別テーブルは参考表示のみ）
   - **片方だけ配信死亡モード** → method の比較と 80/20 は**生きている側のアカウントの指標だけ**で行う
2. **提案は構造実験だけ**: タイトル個別化 / 型変更 / ジャンル変更。各提案に「何を変えるか / 何で測るか / 14 日後の合格ライン」を書く
3. **このリポの制約**: ジャンル・型は変えてよいが、**販促（自家焙煎豆の購入への導線）の目的は維持**。候補は豆の購入に繋がる型に限る（例: レシピ、焙煎の裏側、産地×味の比較、商品ができるまで）
4. **投稿は止めない**。ネタ調査・原稿生成は通常どおり。型の切替は人間が決めて PR で入れる（ルーチンは提案まで）

### 日次レポート（`docs/pdca/$TODAY.md`）の冒頭に書く節

タイトル直下に次の節を書く。判定日なら「ジャンル判定」、配信死亡モードのアカウントがある日は「構造実験の提案」も書く。

```markdown
## ジャンル試行の状態

| アカウント | 試行 # | 開始日 | 経過日 | 判定窓 (n) | 判定指標の現在値 | モード | 次の判定日 |
|---|---|---|---|---|---|---|---|
| IG @open_ground_coffee_roasters | #0 | 2026-09-14 | Day N / 14（判定日なら「判定日」） | MM-DD..MM-DD (n=…) | views 中央値 … / 保存合計 … | 通常 / 切替候補 / 配信死亡モード（YYYY-MM-DD〜） | YYYY-MM-DD |
| YT OPEN GROUND coffee roasters | #0 | 2026-09-14 | … | MM-DD..MM-DD (n=…) | views 中央値 … | … | YYYY-MM-DD |

- 今日の判定: なし（判定日ではない） / 判定日 →「ジャンル判定」節 / 配信死亡モードの新規宣言・解除
- 今日の method 方針: 通常（Meta PDCA・80/20） / 性能データで選ばない（2 アカウントとも配信死亡モード） / <生きている側のアカウント> の指標だけで選ぶ

## ジャンル判定（判定日のみ）
- <アカウント>: 続行 / 切替候補 / 配信死亡 — 根拠（指標値と閾値） — 次ジャンル候補 2〜3 案（切替候補・配信死亡のとき。このリポの制約を守る）

## 構造実験の提案（配信死亡モードのアカウントがある日のみ）
- <アカウント>: <タイトル個別化 / 型変更 / ジャンル変更> — 何を変えるか / 何で測るか / 14 日後の合格ライン
```

## 売りたいもの

**スペシャリティコーヒー豆の焙煎品**。自前EC（構築中）から直販。
カフェ卸も並行して継続（卸経由で知ってもらう入口にも機能）。

## 運用フォーマット: ニュース中心

**「毎朝チェックしないと乗り遅れる」FOMO を作る**ために、コンテンツはニュース性を主軸とする。エバーグリーン豆知識は単体では習慣化されにくい（いつ見てもいい = 見ない）。

| 曜日 | コンテンツ型 | 優先度 |
|---|---|---|
| 月〜金 | **毎朝のコーヒーニュース**（市況、産地、業界動向） | Primary |
| 土・日 | **エバーグリーン深掘り**（焙煎、産地、抽出） | Secondary |

週末エバーグリーンは「今週出たニュースの背景を深く知る」接続を意識する（ニュース駆動ニュース系情報として残る）。

## ペルソナ（優先順）

### Primary: 家で淹れる趣味層
- 既にコーヒー器具（ドリッパー、ミル）を所有
- 1 袋 ¥1,500〜¥3,000 に抵抗がない
- 産地・精製方法・ロースト日などの情報で買う豆を決める
- 失敗した淹れ方のリカバリに興味がある
- **この層を取れなければ事業成立しない**

### Secondary: 開業したての小規模カフェ店主
- 卸先として直接的な収益
- 1 店舗が決まると月5〜15kg の安定需要
- SNS は広告ではなく「焙煎所を知ってもらう信頼シグナル」として機能

### Tertiary: ギフト需要
- おしゃれなビン/パッケージが意思決定に効く
- 父の日、誕生日、開業祝いなど季節キャンペーンで拾う

## 差別化ポイント

1. **スペシャリティにこだわる知識量** — 焙煎士が一次情報を発信
2. **ビン・パッケージの意匠** — 贈答にそのまま使える
3. **SNS で焙煎士の顔が見える** — ECの店主が誰かわかる安心感

## コンテンツ柱（ニュース中心ver）

| 柱 | 比率 | 型 | ねらい |
|---|---|---|---|
| ①**市況ニュース** | 25% | News | C-price動向、為替、輸入原料価格の変化を人間語に翻訳 |
| ②**産地・ハーベストニュース** | 25% | News | 主要産地（エチオピア/ブラジル/コロンビア等）の収穫・気候ニュース |
| ③**業界・ビジネスニュース** | 20% | News | 大手/スタートアップ動向、サステナ、技術、カフェ業界 |
| ④**焙煎士の一次情報** | 15% | Evergreen | 既存の差別化軸、週末に寄せる |
| ⑤**OG入荷・裏側** | 15% | Product | 新入荷、ビン/パッケージ、卸エピソード（販売導線） |

### 具体ネタ例
- ①: 「アラビカ先物が〇%下落、豆価格への影響」「円安で輸入コストどう変わる」
- ②: 「エチオピアの今年の収穫、昨年比で何が変わった」「ブラジル干ばつの最新評価」
- ③: 「Blue Bottle の新戦略」「サステナ認証制度の最新動向」「某カフェチェーンが〇〇発表」
- ④: 「同じ豆、焙煎度3段階で飲み比べ」「浅煎り→深煎りで味がこう変わる理由」
- ⑤: 「今週入荷したエチオピア」「ビンラベルのデザイン誕生秘話」

## 探索方針（Discovery Strategy）

### 英語優先原則

**英語ソースを先に見る**。日本語メディアは英語を翻訳する形で1日遅れる。先に英語で拾い、日本語視聴者向けに翻訳・噛み砕く方が速報性・独自性・視聴者への価値で勝る。
日本語ソースは補完（英語で見つからないドメスティック情報や地域事情用）。

### 推奨ソース（シード）

**英語（優先）**:
- Perfect Daily Grind — 業界トピックの深堀り
- Daily Coffee News (Roast Magazine) — 業界ニュース
- SCA News (sca.coffee) — スペシャリティ協会公式
- Bloomberg / Reuters — 商品市況（Arabica Futures, Coffee C）
- World Coffee Research — 研究・品種
- Global Coffee Report — 国際動向
- Reddit r/Coffee, r/roasting — コミュニティ声

**日本語（バックアップ）**:
- SCAJ（日本スペシャルティコーヒー協会）公式発信
- 業界誌（カフェ＆レストラン、Coffee magazine JP）
- X（Twitter）日本語バリスタ界隈

### Discovery Methods（カテゴリタグ）

| タグ | 説明 |
|---|---|
| `news-en` | 英語の業界メディア・ニュースサイト |
| `news-ja` | 日本語の業界メディア |
| `market-data` | 商品市況、為替、指標データ |
| `official-src` | 産地機関、協会、WCR 等の公式発信 |
| `social-en` | Reddit / X（英語）のコミュニティ話題 |
| `social-ja` | X 日本語バリスタ・焙煎士界隈 |
| `trends` | Google Trends, X Trending |
| `expert-blog` | 業界有名人の個人発信・ブログ |

### 80/20 Exploit/Explore

- 過去14日で視聴率上位3位の method は 80% の頻度で使う（exploit）
- 残り 20% は**新しい method を試す**（explore）。探せば新カテゴリが見つかるはず
- Explore でヒットした method は次サイクルで exploit 側に昇格

### Meta PDCA（探し方の PDCA）

> 冒頭の「ジャンル実験」節が優先する。**配信死亡モードのアカウントの数字では、以下の method 比較・80/20・凍結をしない**（2 アカウントとも配信死亡モードなら性能データで method を選ばない。片方だけなら生きている側の指標だけで比較する）。

毎日のルーチンで以下を分析:

1. 直近 14 日の `performance-history.json` から **method → 平均 views** テーブルを作成
   - YouTube は `stats.views`、Instagram Reels は `instagram.views`（加えて `reach` / `shares` / `saved`）を**別列で並べる**。両者を合算しない（計測定義が違う）
   - `instagram` が `null` の動画は IG 投稿と突き合わせ不能だった回。IG 側の平均から除外する
   - IG insights は最大 48 時間遅れる。前日・当日分の IG 値は暫定として扱う
2. TOP 3 / WORST 3 method を特定
3. 「今日はどの method で探すか」を 80/20 ルールで決める
4. 新しい method のアイデアがあれば PDCA レポートの「Method 提案」セクションに記録
5. **WORST method は 2 週連続なら一旦凍結**（リストから外す、別 method 優先）

## NG パターン

| NG | 理由 |
|---|---|
| 「コーヒーあるある」「コーヒー雑学」 | 通りすがりが集まるが、豆を買う層ではない |
| 「モーニングルーティン」系 | 差別化不能、競合だらけ |
| 「インスタント vs ドリップ」的な初心者解説 | ターゲット層には幼稚に映る |
| EC が未完成の段階での強い購買 CTA | 失望させる。まず「焙煎所フォローして待つ価値」を積む |
| 産地タグの使い回し（水の話で "産地" など） | 動的ラベルを使う |
| **1週間以上前のニュースを新着扱い** | 鮮度が命。`freshness_hours > 168` は原則 NG |
| **出典不明ニュース** | 信用毀損。ソース URL を必ず記録・明示する |

## ファネル

```
SNS 視聴（今朝のコーヒーニュース）
  ↓ 「この人の情報が早くて深い」信頼蓄積
プロフィール bio（「焙煎士が毎日発信 / 豆のご購入はこちら」）
  ↓ EC URL タップ
自前 EC
  ↓ 初回購入（「入荷ニュース」で欲しくなる）
定期便 or 2 回目購入
  ↓ リピート
熱心なファン化（UGC投稿、紹介）
```

**EC 完成までの暫定 CTA**: 「EC 準備中です。フォローしてお待ちください」。
構築中は興味関心の貯金期間と割り切る。

## 目標（初期ドラフト）

| 期限 | 指標 | 数値 |
|---|---|---|
| 3 ヶ月後（2026-07） | 合算フォロワー | 1,000 |
| 3 ヶ月後 | EC 初月の注文数 | 20 件 |
| 6 ヶ月後（2026-10） | 月商 | ¥300,000 |
| 6 ヶ月後 | カフェ卸先 | +2 店舗 |
| 1 年後（2027-04） | 月商 | ¥800,000 |
| 1 年後 | 定期便契約数 | 50 件 |

※ 単価 ¥2,500 想定。EC 完成時点で再計算。

## KPI ダッシュボード（毎週チェック）

- 保存数 / 動画
- プロフィールアクセス数
- EC 遷移数（EC 側で UTM 付与して計測）
- 注文件数 / 平均注文単価
- 定期便契約率
- **Method 別 views 平均**（meta PDCA用。YouTube / Instagram を別々に）
- IG Reels の保存数・シェア数・リーチ（`performance-history.json` の `instagram.saved` / `shares` / `reach`）

### performance-history.json の指標

| キー | 取得元 | 更新 |
|---|---|---|
| `stats.views` / `likes` / `comments` | YouTube Data API v3 | `fetch-stats.mjs`（直近 14 日） |
| `instagram.mediaId` | 投稿時に `upload-instagram.mjs` → `record-upload.mjs` が記録。無い回は `fetch-stats.mjs` が IG 投稿一覧と JST 投稿日で突き合わせて復元 | 投稿時 / 初回取得時 |
| `instagram.views` / `reach` / `likes` / `comments` / `shares` / `saved` | Instagram Graph API `/{ig-media-id}/insights`（`plays` は廃止済みのため `views` を使用） | `fetch-stats.mjs`（直近 14 日 + 未取得の回） |

手動で更新だけしたいときは `gh workflow run fetch-stats.yml`（動画生成・投稿はしない）。

## データスキーマ拡張（enriched-coffee-news.json）

ニュース駆動 + Discovery 記録のため、ルートレベルに `discovery` ブロックを追加:

```json
{
  "date": "2026-04-21",
  "discovery": {
    "method": "news-en",
    "description": "Perfect Daily Grind で今週のトップ記事を確認",
    "sources": ["https://perfectdailygrind.com/..."],
    "query": "arabica price april 2026",
    "freshness_hours": 14
  },
  "articles": [ ... ]
}
```

これを `record-upload.mjs` が `performance-history.json` に合流させ、後続の meta PDCA で使う。

## TBD（ユーザー側で決まり次第反映）

- [ ] EC サイト URL（構築中）
- [ ] 商品ラインナップ詳細（SKU 一覧、価格、容量）
- [ ] 送料設定
- [ ] 定期便の有無と割引率
- [ ] ギフト対応（ラッピング、メッセージカード）
- [ ] 卸価格表（Secondary ペルソナ向け）
